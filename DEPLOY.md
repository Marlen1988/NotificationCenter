# 📋 Пошаговая инструкция деплоя на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте [github.com](https://github.com)
2. Нажмите **"New"** или **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `notification-center` (или любое другое имя)
   - **Description**: `Единый центр уведомлений с элегантным UI`
   - **Public** / **Private**: выберите по желанию
   - ❌ НЕ добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите **"Create repository"**

## Шаг 2: Загрузите файлы проекта

### Вариант А: Через Git (командная строка)

```bash
# Перейдите в папку проекта
cd /path/to/notification-center

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: notification center"

# Добавьте remote (замените YOUR-USERNAME и REPO-NAME)
git remote add origin https://github.com/YOUR-USERNAME/notification-center.git

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

### Вариант Б: Через веб-интерфейс GitHub

1. На странице вашего нового репозитория нажмите **"uploading an existing file"**
2. Перетащите все файлы проекта:
   - `index.html`
   - `README.md`
   - `.gitignore`
3. Добавьте commit message: `Initial commit`
4. Нажмите **"Commit changes"**

## Шаг 3: Включите GitHub Pages

1. В вашем репозитории перейдите в **Settings**
2. Слева найдите раздел **Pages**
3. В **Source** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
4. Нажмите **Save**
5. Подождите 1-2 минуты ⏳

## Шаг 4: Получите ссылку на сайт

После активации GitHub Pages вы увидите зелёное сообщение:

```
✅ Your site is published at https://YOUR-USERNAME.github.io/notification-center/
```

**Готово! 🎉** Теперь ваш сайт доступен по этой ссылке.

---

## 🔄 Обновление сайта

Когда вы внесёте изменения в файлы:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт автоматически обновится через 1-2 минуты.

---

## 🚨 Типичные проблемы

### Проблема: 404 Not Found
**Решение**: Подождите 2-3 минуты после активации Pages

### Проблема: Страница не обновляется
**Решение**: Очистите кеш браузера (Ctrl+Shift+R / Cmd+Shift+R)

### Проблема: CSS не загружается
**Решение**: Проверьте, что в `index.html` используется CDN Tailwind CSS

---

## 📧 Нужна помощь?

Создайте Issue в репозитории или напишите мне!
